apiVersion: v1
kind: Service
metadata:
  name: lesson
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lesson"
    port: 8080
  selector:
    app: lesson
