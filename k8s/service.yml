apiVersion: v1
kind: Service
metadata:
  name: lino
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lino"
    port: 8080
  selector:
    app: lino
