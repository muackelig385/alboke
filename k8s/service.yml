apiVersion: v1
kind: Service
metadata:
  name: keco
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "keco"
    port: 8080
  selector:
    app: keco
