apiVersion: v1
kind: Service
metadata:
  name: dgeuf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dgeuf"
    port: 8080
  selector:
    app: dgeuf
