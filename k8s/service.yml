apiVersion: v1
kind: Service
metadata:
  name: eughtf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "eughtf"
    port: 8080
  selector:
    app: eughtf
